package algorithms

import "gogeneticwrsp/model"

type Greedy struct {
}

func (g *Greedy) Schedule(clouds []model.Cloud, apps []model.Application) (model.Solution, error) {
	return model.Solution{}, nil
}
